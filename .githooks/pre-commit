#!/bin/bash
# Two gates, both cheap, both catching things that only hurt after they are public.
ROOT="$(git rev-parse --show-toplevel)"
python3 "$ROOT/build/leak-scan.py"    || exit 1
python3 "$ROOT/build/licence-guard.py" || exit 1
